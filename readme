1.setting up database and performing CRUD OPERATIONS
1.1 get /listings to view all listings
    get /listings/:id (show route) specific listing ka data view 
    show.ejs num.toLocalString(en-in) commas aajyenge istand
    create (new & create route) get->/listings/new->form->subm->postreq to /listings
    update (edit & update route) get /listings/:id/edit -> edit form -> submit -> PUT /listings/:id
    Delete req to /listing/:id      

    earlier show.ejs like this now bs card
        <ul>
            <li><%= listing.title %></li>
            <li><%=listing.description %></li>
            <li> &#8377; <%=listing.price.toLocaleString("en-IN") %></li> <!--NO. KO STRING M CHANGE KRTA HAI COMMAS DALTA ACC. TO STAND.-->
            <li><%=listing.location %></li>
            <li><%=listing.country %></li>
        </ul>